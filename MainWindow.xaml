<Window x:Class="FoulzExternal.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:FoulzExternal"
        xmlns:explorer="clr-namespace:FoulzExternal.features.games.universal.explorer"
        xmlns:playerlist="clr-namespace:FoulzExternal.features.games.universal.playerlist"
        Title="AXIOM X" Height="650" Width="850" 
        WindowStartupLocation="CenterScreen" 
        ResizeMode="NoResize" 
        Background="Transparent" 
        AllowsTransparency="True" 
        WindowStyle="None"
        Loaded="startvibing">

    <Window.Resources>
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Color x:Key="AccentColor">#FFFFFFFF</Color>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>

        <Style x:Key="GlowClose" TargetType="Button">
            <Setter Property="Background" Value="#151515"/>
            <Setter Property="Foreground" Value="#666"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#202020"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetProperty="(Button.Foreground).(SolidColorBrush.Color)" To="White" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="b" Storyboard.TargetProperty="Opacity" To="0.8" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="tabz_cuh" TargetType="RadioButton">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Foreground" Value="#666"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="0" x:Name="TabBorder">
                            <Grid>
                                <Rectangle x:Name="AccentLine" HorizontalAlignment="Left" Width="3" Fill="{DynamicResource AccentBrush}" Visibility="Collapsed"/>
                                <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="20,0,0,0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter TargetName="AccentLine" Property="Visibility" Value="Visible"/>
                                <Setter Property="Background" Value="#0D0D0D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="toggle_vibe" TargetType="ToggleButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Bg" Background="#111" CornerRadius="1" BorderBrush="#222" BorderThickness="1">
                            <TextBlock x:Name="Txt" Text="{TemplateBinding Content}" FontSize="9" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#555" FontWeight="Bold"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bg" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="Txt" Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="Bg" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="{DynamicResource AccentColor}" BlurRadius="8" ShadowDepth="0" Opacity="0.9"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter TargetName="Bg" Property="BorderBrush" Value="#222"/>
                                <Setter TargetName="Txt" Property="Foreground" Value="#555"/>
                                <Setter TargetName="Bg" Property="Effect" Value="{x:Null}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="slider_chill" TargetType="Slider">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Border Height="2" Background="#1A1A1A" VerticalAlignment="Center"/>
                            <Track x:Name="PART_Track">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate>
                                                <Border Width="12" Height="12" Background="{DynamicResource AccentBrush}" CornerRadius="0" BorderBrush="Black" BorderThickness="1"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="axiom_btn_bruh" TargetType="Button">
            <Setter Property="Background" Value="#111"/>
            <Setter Property="Foreground" Value="#AAA"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="#222" BorderThickness="1" CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#151515"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="small_btn" TargetType="Button" BasedOn="{StaticResource axiom_btn_bruh}">
            <Setter Property="Width" Value="90"/>
            <Setter Property="Height" Value="26"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>

        <Style x:Key="combo_chill" TargetType="ComboBox">
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Background" Value="#0A0A0A"/>
            <Setter Property="Foreground" Value="#DDD"/>
            <Setter Property="BorderBrush" Value="#222"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="HorizontalContentAlignment" Value="Right"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Bd"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="3">
                                <DockPanel LastChildFill="True" Margin="4,0">
                                    <ContentPresenter x:Name="ContentSite"
                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                      Content="{TemplateBinding SelectionBoxItem}"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Right"
                                      Margin="0,0,4,0"/>
                                    <ToggleButton x:Name="ToggleButton"
                                          DockPanel.Dock="Right"
                                          Width="28"
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          Padding="0"
                                          HorizontalAlignment="Right"
                                          OverridesDefaultStyle="True"
                                          FocusVisualStyle="{x:Null}">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">

                                                <Border x:Name="TbRoot"
                                              Background="Transparent"
                                              Width="{TemplateBinding Width}"
                                              Height="{TemplateBinding Height}"
                                              CornerRadius="2">

                                                    <Grid>
                                                        <Path x:Name="Arrow"
                                                      Data="M 0 0 L 4 4 L 8 0 Z"
                                                      Fill="{DynamicResource AccentBrush}"
                                                      Stretch="Uniform"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                                    </Grid>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="TbRoot" Property="Background" Value="#202020"/>
                                                        <Setter TargetName="Arrow" Property="Fill" Value="White"/>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter TargetName="TbRoot" Property="Background" Value="#222"/>
                                                        <Setter TargetName="Arrow" Property="Fill" Value="White"/>
                                                    </Trigger>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="TbRoot" Property="Background" Value="#161616"/>
                                                        <Setter TargetName="Arrow" Property="Fill" Value="{DynamicResource AccentBrush}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                </DockPanel>
                            </Border>

                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade"
                                   MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                   StaysOpen="False">
                                <Border Background="#0A0A0A" BorderBrush="#222" BorderThickness="1" CornerRadius="3" Padding="4">
                                    <ScrollViewer CanContentScroll="True" HorizontalScrollBarVisibility="Disabled">
                                        <StackPanel IsItemsHost="True"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#DDD"/>
                        <Setter Property="Padding" Value="6,6"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#0F0F0F"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#1E1E1E"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <Storyboard x:Key="FadeInSequence">
            <DoubleAnimation Storyboard.TargetName="MainContent" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5"/>
        </Storyboard>

        <Storyboard x:Key="PulseGlow" RepeatBehavior="Forever" AutoReverse="True">
            <DoubleAnimation Storyboard.TargetName="OuterGlow" Storyboard.TargetProperty="BlurRadius" From="8" To="18" Duration="0:0:2.8" />
            <DoubleAnimation Storyboard.TargetName="OuterGlow" Storyboard.TargetProperty="Opacity" From="0.08" To="0.28" Duration="0:0:2.8" />
        </Storyboard>

        <Storyboard x:Key="InlineStatusFadeIn"/>
    </Window.Resources>

    <Border Background="#080808" BorderBrush="#1A1A1A" BorderThickness="1" CornerRadius="6" Margin="10">
        <Border.Effect>
            <DropShadowEffect x:Name="OuterGlow" BlurRadius="12" Color="{DynamicResource AccentColor}" Opacity="0.18" ShadowDepth="0"/>
        </Border.Effect>
        <Grid x:Name="MainContent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#050505" BorderBrush="#151515" BorderThickness="0,0,1,0" CornerRadius="6,0,0,6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="20,25,20,20">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Axiom X
                                       " FontSize="26" FontWeight="Black" Foreground="White"/>
                            <TextBlock Text="C#" FontSize="26" FontWeight="Black" Foreground="{DynamicResource AccentBrush}" Margin="5,0,0,0"/>
                        </StackPanel>
                        <TextBlock Text="👅discord.gg/rbw5
                                   👅" FontSize="9" Foreground="#444" FontWeight="Bold" Margin="2,-2,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1">
                        <RadioButton x:Name="AimbotTab" Style="{StaticResource tabz_cuh}" IsChecked="True">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🎯" Margin="0,0,8,0" FontSize="14"/>
                                <TextBlock Text="AIMBOT" VerticalAlignment="Center"/>
                            </StackPanel>
                        </RadioButton>
                        <RadioButton x:Name="SilentTab" Style="{StaticResource tabz_cuh}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🤫" Margin="0,0,8,0" FontSize="14"/>
                                <TextBlock Text="SILENT" VerticalAlignment="Center"/>
                            </StackPanel>
                        </RadioButton>
                        <RadioButton x:Name="VisualsTab" Style="{StaticResource tabz_cuh}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="👁️" Margin="0,0,8,0" FontSize="14"/>
                                <TextBlock Text="VISUALS" VerticalAlignment="Center"/>
                            </StackPanel>
                        </RadioButton>
                        <RadioButton x:Name="ChecksTab" Style="{StaticResource tabz_cuh}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="✅" Margin="0,0,8,0" FontSize="14"/>
                                <TextBlock Text="CHECKS" VerticalAlignment="Center"/>
                            </StackPanel>
                        </RadioButton>
                        <RadioButton x:Name="MiscTab" Style="{StaticResource tabz_cuh}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🧰" Margin="0,0,8,0" FontSize="14"/>
                                <TextBlock Text="MISC" VerticalAlignment="Center"/>
                            </StackPanel>
                        </RadioButton>
                        <RadioButton x:Name="ExplorerTab" Style="{StaticResource tabz_cuh}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📁" Margin="0,0,8,0" FontSize="14"/>
                                <TextBlock Text="EXPLORER" VerticalAlignment="Center"/>
                            </StackPanel>
                        </RadioButton>
                        <RadioButton x:Name="PlayersTab" Style="{StaticResource tabz_cuh}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="👥" Margin="0,0,8,0" FontSize="14"/>
                                <TextBlock Text="PLAYERS" VerticalAlignment="Center"/>
                            </StackPanel>
                        </RadioButton>
                        <RadioButton x:Name="SettingsTab" Style="{StaticResource tabz_cuh}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⚙️" Margin="0,0,8,0" FontSize="14"/>
                                <TextBlock Text="SETTINGS" VerticalAlignment="Center"/>
                            </StackPanel>
                        </RadioButton>
                    </StackPanel>

                    <Border Grid.Row="2" Background="#030303" Padding="12" CornerRadius="4,4,6,6">
                        <StackPanel>
                            <Border Background="#080808" BorderBrush="#111" BorderThickness="1" Padding="8" Margin="0,0,0,10" CornerRadius="3">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Ellipse x:Name="ProfileEllipse" Width="30" Height="30" Fill="#111" Stroke="#222"/>
                                    <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                                        <TextBlock x:Name="ProfileNameText" Text="USER" FontSize="10" Foreground="White" FontWeight="Bold"/>
                                        <TextBlock Text="AUTHENTICATED" FontSize="8" Foreground="#444"/>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <Button Content="ATTACH" Click="hookitup" Style="{StaticResource axiom_btn_bruh}" Height="30" Margin="0,0,0,10" FontWeight="Bold"/>

                            <StackPanel x:Name="InlineStatusWrap">
                                <Grid Margin="0,0,0,5">
                                    <StackPanel Orientation="Horizontal">
                                        <Ellipse x:Name="StatusIndicator" Width="6" Height="6" Fill="#333" Margin="0,0,8,0"/>
                                        <TextBlock x:Name="StatusText" Text="IDLE" Foreground="#444" FontSize="9" FontWeight="Bold"/>
                                    </StackPanel>
                                    <TextBlock x:Name="PidValueText" Text="N/A" FontSize="9" Foreground="#333" HorizontalAlignment="Right"/>
                                </Grid>

                                <UniformGrid Columns="3" Margin="0,5">
                                    <Ellipse x:Name="CachesIndicator" Width="8" Height="8" Fill="#111" Stroke="#222" ToolTip="Caches"/>
                                    <Ellipse x:Name="StorageIndicator" Width="8" Height="8" Fill="#111" Stroke="#222" ToolTip="Storage"/>
                                    <Ellipse x:Name="VmmIndicator" Width="8" Height="8" Fill="#111" Stroke="#222" ToolTip="VMM"/>
                                </UniformGrid>

                                <TextBlock x:Name="VersionValueText" Text="VERSION: N/A" FontSize="8" Foreground="#222" HorizontalAlignment="Center" Margin="0,5"/>
                                <TextBlock x:Name="BaseValueText" Text="0x0000000000000000" FontSize="8" Foreground="#222" HorizontalAlignment="Center" FontFamily="Consolas"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <Grid Grid.Column="1" Margin="25">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Button Grid.Row="0" Content="✕" HorizontalAlignment="Right" Click="bye" Style="{StaticResource GlowClose}" Cursor="Hand" Width="34" Height="28" Margin="0,3,3,3">
                    <Button.Effect>
                        <DropShadowEffect BlurRadius="10" Color="{DynamicResource AccentColor}" Opacity="0.6" ShadowDepth="0"/>
                    </Button.Effect>
                </Button>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden">
                    <Grid>
                        <StackPanel Visibility="{Binding IsChecked, ElementName=AimbotTab, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Text="AIMBOT" Foreground="#333" FontSize="10" FontWeight="Bold" Margin="0,0,0,15"/>

                            <UniformGrid Columns="2">
                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,5,10" CornerRadius="4">
                                    <Grid>
                                        <TextBlock Text="AIMBOT" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <ToggleButton x:Name="AimbotToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="OFF" Click="aimbottgl"/>
                                    </Grid>
                                </Border>

                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="5,0,0,10" CornerRadius="4">
                                    <Grid>
                                        <TextBlock Text="TOGGLE TYPE" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <ComboBox x:Name="ToggleTypeCombo" HorizontalAlignment="Right" Width="120" Style="{StaticResource combo_chill}" SelectionChanged="toggletype">
                                            <ComboBoxItem Content="Hold"/>
                                            <ComboBoxItem Content="Toggle"/>
                                        </ComboBox>
                                    </Grid>
                                </Border>
                            </UniformGrid>

                            <UniformGrid Columns="2">
                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,5,10" CornerRadius="4">
                                    <StackPanel>
                                        <TextBlock Text="AIM TYPE" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <ComboBox x:Name="AimingTypeCombo" HorizontalAlignment="Right" Width="120" Style="{StaticResource combo_chill}" SelectionChanged="aimingtype">
                                            <ComboBoxItem Content="Camera"/>
                                            <ComboBoxItem Content="Mouse"/>
                                        </ComboBox>
                                    </StackPanel>
                                </Border>

                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="5,0,0,10" CornerRadius="4">
                                    <StackPanel>
                                        <TextBlock Text="TARGET BONE" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <ComboBox x:Name="TargetBoneCombo" HorizontalAlignment="Right" Width="120" Style="{StaticResource combo_chill}" SelectionChanged="targetbone">
                                            <ComboBoxItem Content="Head"/>
                                            <ComboBoxItem Content="HumanoidRootPart"/>
                                            <ComboBoxItem Content="Left Arm"/>
                                            <ComboBoxItem Content="Right Arm"/>
                                            <ComboBoxItem Content="Left Leg"/>
                                            <ComboBoxItem Content="Right Leg"/>
                                        </ComboBox>
                                    </StackPanel>
                                </Border>
                            </UniformGrid>

                            <UniformGrid Columns="2">
                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,5,10" CornerRadius="4">
                                    <Grid>
                                        <TextBlock Text="KEYBIND" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <Button x:Name="AimbotKeyButton" HorizontalAlignment="Right" Width="120" Style="{StaticResource small_btn}" Click="aimbotkey">SET</Button>
                                    </Grid>
                                </Border>

                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="5,0,0,10" CornerRadius="4">
                                    <Grid>
                                        <TextBlock Text="STICKY AIM" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <ToggleButton x:Name="StickyAimToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="OFF" Click="stickyaim"/>
                                    </Grid>
                                </Border>
                            </UniformGrid>

                            <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,0,10" CornerRadius="4">
                                <StackPanel>
                                    <TextBlock Text="SENSITIVITY" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                    <Slider x:Name="SensitivitySlider" Style="{StaticResource slider_chill}" Minimum="0.1" Maximum="5" Value="1" ValueChanged="sensitivity" Margin="0,5,45,0"/>
                                    <TextBlock x:Name="SensitivityValueText" Text="1.0" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Right" FontSize="10" FontWeight="Bold"/>
                                </StackPanel>
                            </Border>

                            <UniformGrid Columns="2">
                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,5,10" CornerRadius="4">
                                    <StackPanel>
                                        <TextBlock Text="SMOOTHNESS" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <ToggleButton x:Name="SmoothnessToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="OFF" Click="smoothness"/>
                                        <Slider x:Name="SmoothnessXSlider" Style="{StaticResource slider_chill}" Minimum="0" Maximum="1" Value="0" ValueChanged="smoothx" Margin="0,8,45,0"/>
                                        <Slider x:Name="SmoothnessYSlider" Style="{StaticResource slider_chill}" Minimum="0" Maximum="1" Value="0.05" ValueChanged="smoothy" Margin="0,8,45,0"/>
                                    </StackPanel>
                                </Border>

                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="5,0,0,10" CornerRadius="4">
                                    <StackPanel>
                                        <TextBlock Text="PREDICTION" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <ToggleButton x:Name="PredictionToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="OFF" Click="prediction"/>
                                        <Slider x:Name="PredictionXSlider" Style="{StaticResource slider_chill}" Minimum="0" Maximum="4" Value="2" ValueChanged="predx" Margin="0,8,45,0"/>
                                        <Slider x:Name="PredictionYSlider" Style="{StaticResource slider_chill}" Minimum="0" Maximum="4" Value="2" ValueChanged="predy" Margin="0,8,45,0"/>
                                    </StackPanel>
                                </Border>
                            </UniformGrid>

                            <UniformGrid Columns="2">
                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,5,10" CornerRadius="4">
                                    <StackPanel>
                                        <TextBlock Text="Aimbot FOV" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <Slider x:Name="AimbotFovSlider" Style="{StaticResource slider_chill}" Minimum="10" Maximum="1000" Value="100" ValueChanged="aimbotfov" Margin="0,5,45,0"/>
                                        <TextBlock x:Name="AimbotFovValueText" Text="100" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Right" FontSize="10" FontWeight="Bold"/>
                                    </StackPanel>
                                </Border>
                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="5,0,0,10" CornerRadius="4">
                                    <StackPanel>
                                        <TextBlock Text="RANGE" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <Slider x:Name="RangeSlider" Style="{StaticResource slider_chill}" Minimum="10" Maximum="1000" Value="100" ValueChanged="range" Margin="0,5,45,0"/>
                                        <TextBlock x:Name="RangeValueText" Text="100" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Right" FontSize="10" FontWeight="Bold"/>
                                    </StackPanel>
                                </Border>
                            </UniformGrid>

                            <UniformGrid Columns="2">
                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,5,10" CornerRadius="4">
                                    <Grid>
                                        <TextBlock Text="SHOW FOV" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                            <ToggleButton x:Name="AnimatedFovToggle" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="ANIM" Click="animatedfov"/>
                                            <ToggleButton x:Name="ShowFovToggle" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="ON" Click="showfov" Margin="8,0,0,0"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="5,0,0,10" CornerRadius="4">
                                    <Grid>
                                        <TextBlock Text="FILL FOV" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <ToggleButton x:Name="FillFovToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="OFF" Click="fillfov"/>
                                    </Grid>
                                </Border>
                            </UniformGrid>

                        </StackPanel>

                        <StackPanel Visibility="{Binding IsChecked, ElementName=SilentTab, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Text="SILENT AIM" Foreground="#333" FontSize="10" FontWeight="Bold" Margin="0,0,0,15"/>

                            <UniformGrid Columns="2">
                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,5,10" CornerRadius="4">
                                    <Grid>
                                        <TextBlock Text="SILENT AIMBOT" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <ToggleButton x:Name="SilentAimbotToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="OFF" Click="silentaimbottgl"/>
                                    </Grid>
                                </Border>

                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="5,0,0,10" CornerRadius="4">
                                    <Grid>
                                        <TextBlock Text="ALWAYS ON" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <ToggleButton x:Name="SilentAlwaysOnToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="OFF" Click="silentalwaysontgl"/>
                                    </Grid>
                                </Border>
                            </UniformGrid>

                            <UniformGrid Columns="2">
                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,5,10" CornerRadius="4">
                                    <Grid>
                                        <TextBlock Text="KEYBIND" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <Button x:Name="SilentAimbotKeyButton" HorizontalAlignment="Right" Width="120" Style="{StaticResource small_btn}" Click="silentkey">SET</Button>
                                    </Grid>
                                </Border>

                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="5,0,0,10" CornerRadius="4">
                                    <Grid>
                                        <TextBlock Text="VISUALIZER" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <ToggleButton x:Name="SilentVisualizerToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="OFF" Click="silentvisualizertgl"/>
                                    </Grid>
                                </Border>
                            </UniformGrid>

                            <UniformGrid Columns="2">
                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,5,10" CornerRadius="4">
                                    <Grid>
                                        <TextBlock Text="SHOW FOV" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <ToggleButton x:Name="SilentShowFovToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="ON" Click="silentshowfovtgl"/>
                                    </Grid>
                                </Border>

                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="5,0,0,10" CornerRadius="4">
                                    <StackPanel>
                                        <TextBlock Text="SILENT FOV" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <Slider x:Name="SilentFovSlider" Style="{StaticResource slider_chill}" Minimum="10" Maximum="1000" Value="150" ValueChanged="silentfov" Margin="0,5,45,0"/>
                                        <TextBlock x:Name="SilentFovValueText" Text="150" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Right" FontSize="10" FontWeight="Bold"/>
                                    </StackPanel>
                                </Border>
                            </UniformGrid>

                            <UniformGrid Columns="2">
                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,5,10" CornerRadius="4">
                                    <StackPanel>
                                        <TextBlock Text="PREDICTION X" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <Slider x:Name="SilentPredictionXSlider" Style="{StaticResource slider_chill}" Minimum="0" Maximum="4" Value="2" ValueChanged="silentpredx" Margin="0,5,45,0"/>
                                        <TextBlock x:Name="SilentPredictionXValueText" Text="2" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Right" FontSize="10" FontWeight="Bold"/>
                                    </StackPanel>
                                </Border>

                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="5,0,0,10" CornerRadius="4">
                                    <StackPanel>
                                        <TextBlock Text="PREDICTION Y" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <Slider x:Name="SilentPredictionYSlider" Style="{StaticResource slider_chill}" Minimum="0" Maximum="4" Value="2" ValueChanged="silentpredy" Margin="0,5,45,0"/>
                                        <TextBlock x:Name="SilentPredictionYValueText" Text="2" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Right" FontSize="10" FontWeight="Bold"/>
                                    </StackPanel>
                                </Border>
                            </UniformGrid>

                            <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,0,10" CornerRadius="4">
                                <Grid>
                                    <TextBlock Text="ENABLE PREDICTION" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                    <ToggleButton x:Name="SilentPredictionToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="ON" Click="silentpredictiontgl"/>
                                </Grid>
                            </Border>
                        </StackPanel>

                        <StackPanel Visibility="{Binding IsChecked, ElementName=VisualsTab, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Text="VISUALS" Foreground="#333" FontSize="10" FontWeight="Bold" Margin="0,0,0,15"/>

                            <UniformGrid Columns="2">
                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,5,10" CornerRadius="4">
                                    <Grid>
                                        <TextBlock Text="BOX ESP" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <ToggleButton x:Name="BoxEspToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="OFF" Click="boxtgl"/>
                                    </Grid>
                                </Border>
                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="5,0,0,10" CornerRadius="4">
                                    <Grid>
                                        <TextBlock Text="BOX FILL" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <ToggleButton x:Name="FilledBoxToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="OFF" Click="filltgl"/>
                                    </Grid>
                                </Border>
                            </UniformGrid>

                            <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,0,10" CornerRadius="4">
                                <StackPanel>
                                    <Grid>
                                        <TextBlock Text="TRACERS" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <ToggleButton x:Name="TracersToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="OFF" Click="tracetgl"/>
                                    </Grid>
                                    <Grid x:Name="TracerConfig" Margin="0,15,0,0" Visibility="Visible">
                                        <Slider x:Name="TracerThicknessSlider" Style="{StaticResource slider_chill}" Minimum="0.5" Maximum="6" Value="1.5" ValueChanged="tracew" Margin="0,0,45,0"/>
                                        <TextBlock x:Name="TracerThicknessValueText" Text="1.5" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Right" FontSize="10" FontWeight="Bold"/>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,0,10" CornerRadius="4">
                                <StackPanel>
                                    <Grid>
                                        <TextBlock Text="NAME ESP" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <ToggleButton x:Name="NameToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="OFF" Click="nametgl"/>
                                    </Grid>
                                    <Grid x:Name="NameConfig" Margin="0,15,0,0">
                                        <Slider x:Name="NameSizeSlider" Style="{StaticResource slider_chill}" Minimum="8" Maximum="32" Value="12" ValueChanged="namesize" Margin="0,0,45,0"/>
                                        <TextBlock x:Name="NameSizeValueText" Text="12" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Right" FontSize="10" FontWeight="Bold"/>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,0,10" CornerRadius="4">
                                <StackPanel>
                                    <Grid>
                                        <TextBlock Text="DISTANCE DISPLAY" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <ToggleButton x:Name="DistanceToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="OFF" Click="disttgl"/>
                                    </Grid>
                                    <Grid x:Name="DistanceConfig" Margin="0,15,0,0">
                                        <Slider x:Name="DistanceSizeSlider" Style="{StaticResource slider_chill}" Minimum="8" Maximum="32" Value="15" ValueChanged="distsize" Margin="0,0,45,0"/>
                                        <TextBlock x:Name="DistanceSizeValueText" Text="15" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Right" FontSize="10" FontWeight="Bold"/>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <UniformGrid Columns="2">
                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,5,10" CornerRadius="4">
                                    <Grid>
                                        <TextBlock Text="SKELETON" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <ToggleButton x:Name="SkeletonToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="OFF" Checked="skeletonon" Unchecked="skeletonoff"/>
                                    </Grid>
                                </Border>
                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="5,0,0,10" CornerRadius="4">
                                    <Grid>
                                        <TextBlock Text="HEALTH BAR" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <ToggleButton x:Name="HealthToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="OFF" Checked="healthon" Unchecked="healthoff"/>
                                    </Grid>
                                </Border>
                            </UniformGrid>

                            <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,0,10" CornerRadius="4">
                                <StackPanel>
                                    <Grid>
                                        <TextBlock Text="HEAD INDICATOR" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <ToggleButton x:Name="HeadCircleToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="OFF" Click="circletgl"/>
                                    </Grid>
                                    <Grid x:Name="HeadCircleConfig" Margin="0,15,0,0">
                                        <Slider x:Name="HeadCircleScaleSlider" Style="{StaticResource slider_chill}" Minimum="0.5" Maximum="4" Value="2.5" ValueChanged="circlescale" Margin="0,0,45,0"/>
                                        <TextBlock x:Name="HeadCircleScaleValueText" Text="2.5" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Right" FontSize="10" FontWeight="Bold"/>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <UniformGrid Columns="3">
                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="10" Margin="0,0,5,0" CornerRadius="4">
                                    <StackPanel>
                                        <TextBlock Text="CORNER" Foreground="#555" FontSize="8" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                        <ToggleButton x:Name="CornerEspToggle" Width="40" Height="20" Style="{StaticResource toggle_vibe}" Click="cornertgl"/>
                                    </StackPanel>
                                </Border>
                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="10" Margin="2,0,2,0" CornerRadius="4">
                                    <StackPanel>
                                        <TextBlock Text="HAT" Foreground="#555" FontSize="8" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                        <ToggleButton x:Name="ChinaHatToggle" Width="40" Height="20" Style="{StaticResource toggle_vibe}" Click="hattgl"/>
                                    </StackPanel>
                                </Border>
                                <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="10" Margin="5,0,0,0" CornerRadius="4">
                                    <StackPanel>
                                        <TextBlock Text="SELF" Foreground="#555" FontSize="8" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                        <ToggleButton x:Name="LocalPlayerEspToggle" Width="40" Height="20" Style="{StaticResource toggle_vibe}" Click="localtgl"/>
                                    </StackPanel>
                                </Border>
                            </UniformGrid>
                        </StackPanel>

                        <StackPanel Visibility="{Binding IsChecked, ElementName=ChecksTab, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Text="CHECKS" Foreground="#333" FontSize="10" FontWeight="Bold" Margin="0,0,0,15"/>
                            <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,0,10" CornerRadius="4">
                                <Grid>
                                    <TextBlock Text="TEAM CHECK" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                    <ToggleButton x:Name="GlobalTeamCheckToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="OFF" Click="globalteamcheck"/>
                                </Grid>
                            </Border>
                            <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,0,10" CornerRadius="4">
                                <Grid>
                                    <TextBlock Text="DOWNED CHECK" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                    <ToggleButton x:Name="DownedCheckToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="OFF" Click="downedcheck"/>
                                </Grid>
                            </Border>
                            <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,0,10" CornerRadius="4">
                                <Grid>
                                    <TextBlock Text="TRANSPARENCY CHECK" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                    <ToggleButton x:Name="TransparencyToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="OFF" Click="transparency"/>
                                </Grid>
                            </Border>
                        </StackPanel>

                        <StackPanel Visibility="{Binding IsChecked, ElementName=MiscTab, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Text="LOCALPLAYER" Foreground="#333" FontSize="10" FontWeight="Bold" Margin="0,0,0,15"/>

                            <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,0,10" CornerRadius="4">
                                <StackPanel>
                                    <Grid>
                                        <TextBlock Text="WALK SPEED" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <ToggleButton x:Name="WalkspeedToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Checked="oncheck" Unchecked="offcheck" Content="OFF"/>
                                    </Grid>
                                    <Grid x:Name="WalkConfig" Margin="0,15,0,0">
                                        <Slider x:Name="WalkspeedSlider" Style="{StaticResource slider_chill}" Minimum="16" Maximum="500" Value="100" ValueChanged="speedy" Margin="0,0,45,0"/>
                                        <TextBlock x:Name="WalkspeedValueText" Text="100" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Right" FontSize="10" FontWeight="Bold"/>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,0,10" CornerRadius="4">
                                <StackPanel>
                                    <Grid>
                                        <TextBlock Text="JUMP POWER" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <ToggleButton x:Name="JumpPowerToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Checked="oncheck" Unchecked="offcheck" Content="OFF"/>
                                    </Grid>
                                    <Grid x:Name="JumpConfig" Margin="0,15,0,0">
                                        <Slider x:Name="JumpPowerSlider" Style="{StaticResource slider_chill}" Minimum="50" Maximum="500" Value="100" ValueChanged="jumpy" Margin="0,0,45,0"/>
                                        <TextBlock x:Name="JumpPowerValueText" Text="100" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Right" FontSize="10" FontWeight="Bold"/>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,0,10" CornerRadius="4">
                                <StackPanel>
                                    <Grid>
                                        <TextBlock Text="FOV" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <ToggleButton x:Name="FovToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Checked="oncheck" Unchecked="offcheck" Content="OFF"/>
                                    </Grid>
                                    <Grid x:Name="FovConfig" Margin="0,15,0,0">
                                        <Slider x:Name="FovSlider" Style="{StaticResource slider_chill}" Minimum="70" Maximum="120" Value="90" ValueChanged="fieldofview" Margin="0,0,45,0"/>
                                        <TextBlock x:Name="FovValueText" Text="90" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Right" FontSize="10" FontWeight="Bold"/>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,0,10" CornerRadius="4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Button x:Name="DesyncBindButton" Grid.Column="0" Width="90" Height="26" Style="{StaticResource small_btn}" Click="desynckey">SET</Button>

                                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                        <TextBlock Text="DESYNC" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <TextBlock Text="Visualizer / Toggle" Foreground="#444" FontSize="9"/>
                                    </StackPanel>

                                    <ToggleButton x:Name="DesyncVisualizerToggle" Grid.Column="2" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="VIS" Click="desyncvisualizer" Margin="0,0,8,0"/>
                                    <ToggleButton x:Name="DesyncToggle" Grid.Column="3" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="OFF" Click="desynctgl"/>
                                </Grid>
                            </Border>

                            <Border Background="#0A0A0A" BorderBrush="#111" BorderThickness="1" Padding="12" Margin="0,0,0,10" CornerRadius="4">
                                <StackPanel>
                                    <Grid>
                                        <TextBlock Text="FLIGHT" Foreground="#888" FontSize="10" FontWeight="SemiBold"/>
                                        <ToggleButton x:Name="FlightToggle" HorizontalAlignment="Right" Width="45" Height="22" Style="{StaticResource toggle_vibe}" Content="OFF" Click="flighttgl"/>
                                    </Grid>

                                    <Grid Margin="0,15,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0"
                                                   Text="SPEED"
                                                   Foreground="#888"
                                                   FontSize="10"
                                                   FontWeight="SemiBold"
                                                   VerticalAlignment="Center"/>

                                        <Slider Grid.Column="1"
                                                x:Name="FlightSpeedSlider"
                                                Style="{StaticResource slider_chill}"
                                                Minimum="0.1"
                                                Maximum="10"
                                                Value="1.5"
                                                ValueChanged="flightspeed"
                                                Margin="10,0"
                                                VerticalAlignment="Center"
                                                MinWidth="140"/>

                                        <TextBlock Grid.Column="2"
                                                   x:Name="FlightSpeedValueText"
                                                   Text="{Binding ElementName=FlightSpeedSlider, Path=Value, StringFormat={}{0:0.00}}"
                                                   Foreground="{DynamicResource AccentBrush}"
                                                   HorizontalAlignment="Right"
                                                   VerticalAlignment="Center"
                                                   FontSize="10"
                                                   FontWeight="Bold"
                                                   Margin="8,0,0,0"
                                                   Width="44"
                                                   TextAlignment="Right"/>
                                    </Grid>
                                    <Button x:Name="FlightBindButton" Width="90" Height="26" Style="{StaticResource small_btn}" Click="flightkey">SET</Button>
                                </StackPanel>
                            </Border>
                        </StackPanel>

                        <StackPanel Visibility="{Binding IsChecked, ElementName=ExplorerTab, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,20,0,0">
                            <explorer:ExplorerControl/>
                        </StackPanel>

                        <StackPanel Visibility="{Binding IsChecked, ElementName=PlayersTab, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,20,0,0">
                            <playerlist:PlayerListControl/>
                        </StackPanel>
                        <StackPanel Visibility="{Binding IsChecked, ElementName=SettingsTab, Converter={StaticResource BooleanToVisibilityConverter}}">

                            <TextBlock Text="CONFIGURATION" Foreground="#333" FontSize="10" FontWeight="Bold" Margin="0,0,0,15"/>

                            <StackPanel Margin="0,0,0,30">

                                <TextBlock Text="Config Name:" Foreground="#888" FontSize="10" Margin="0,0,0,5"/>

                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBox x:Name="confignametext"
                                               FocusVisualStyle="{x:Null}"
                                               Grid.Column="0"
                                               Height="30"
                                               Padding="8,0"
                                               VerticalContentAlignment="Center"
                                               Background="#0A0A0A"
                                               BorderBrush="#111"
                                               BorderThickness="1"
                                               Foreground="White"
                                               FontSize="11"
                                               Text="default"/>

                                    <ComboBox x:Name="configlistdropdown"
                                            FocusVisualStyle="{x:Null}"
                                            Grid.Column="0"
                                            Height="30"
                                            Padding="8,0"
                                            VerticalContentAlignment="Center"
                                            Background="#0A0A0A"
                                            BorderBrush="#111"
                                            BorderThickness="1"
                                            Foreground="White"
                                            FontSize="11"
                                            SelectionChanged="cfglistchanged"
                                            Visibility="Collapsed"
                                            HorizontalContentAlignment="Left">  
                                        <ComboBox.Template>
                                            <ControlTemplate TargetType="ComboBox">
                                                <Grid>
                                                    <Border x:Name="Bd"
                                                        Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="3">       
                                                        <ContentPresenter x:Name="ContentSite"
                                                                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                                    Content="{TemplateBinding SelectionBoxItem}"
                                                                    VerticalAlignment="Center"
                                                                    HorizontalAlignment="Left"
                                                                    Margin="8,0,4,0"/>
                                                                </Border>
                                                                <Popup x:Name="Popup"
                                                                    Placement="Bottom"
                                                                    IsOpen="{TemplateBinding IsDropDownOpen}"
                                                                    AllowsTransparency="True"
                                                                    Focusable="False"
                                                                    PopupAnimation="Fade"
                                                                    MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                                                    StaysOpen="False">
                                                        <Border Background="#0A0A0A" BorderBrush="#222" BorderThickness="1" CornerRadius="3" Padding="4">
                                                            <ScrollViewer CanContentScroll="True" HorizontalScrollBarVisibility="Disabled">
                                                                <StackPanel IsItemsHost="True"/>
                                                            </ScrollViewer>
                                                        </Border>
                                                    </Popup>
                                                </Grid>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsEnabled" Value="False">
                                                        <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Bd" Property="BorderBrush" Value="#222"/>
                                                        <Setter TargetName="Bd" Property="BorderThickness" Value="1"/>
                                                    </Trigger>
                                                    <Trigger Property="IsFocused" Value="True">
                                                        <Setter TargetName="Bd" Property="BorderBrush" Value="#222"/>
                                                        <Setter TargetName="Bd" Property="BorderThickness" Value="1"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </ComboBox.Template>
                                    </ComboBox>

                                    <Button x:Name="toggleconfiglist"
                                              FocusVisualStyle="{x:Null}"
                                              Grid.Column="1"
                                              Content="▼"
                                              Width="30"
                                              Height="30"
                                              Margin="5,0,0,0"
                                              Click="togglecfglist"
                                              Style="{StaticResource axiom_btn_bruh}"/>
                                </Grid>

                                <UniformGrid Columns="3" Margin="0,0,0,10">
                                    <Button Content="SAVE CONFIG" Click="savecfg" Style="{StaticResource axiom_btn_bruh}" Height="35" Margin="0,0,5,0"/>
                                    <Button Content="LOAD CONFIG" Click="loadcfg" Style="{StaticResource axiom_btn_bruh}" Height="35" Margin="5,0,5,0"/>
                                    <Button Content="RESET DEFAULTS" Click="resetcfg" Style="{StaticResource axiom_btn_bruh}" Height="35" Margin="5,0,0,0"/>
                                </UniformGrid>

                                <UniformGrid Columns="2" Margin="0,0,0,10">
                                    <Button Content="SET AS DEFAULT" Click="setdefaultcfg" Style="{StaticResource axiom_btn_bruh}" Height="35" Margin="0,0,5,0"/>
                                    <Button Content="DELETE CONFIG" Click="deletecfg" Style="{StaticResource axiom_btn_bruh}" Height="35" Margin="5,0,0,0"/>
                                </UniformGrid>

                                <TextBlock x:Name="defconfigtext"
                                             Text="No default config set"
                                             Foreground="#666"
                                             FontSize="10"
                                             Margin="0,5,0,0"/>

                                <Button Content="OPEN CONFIG FOLDER"
                                          Click="opencfgfolder"
                                          Style="{StaticResource axiom_btn_bruh}"
                                          Height="30"
                                          Width="180"
                                          HorizontalAlignment="Left"
                                         Margin="0,10,0,0"/>
                            </StackPanel>

                            <TextBlock Text="INTERFACE CUSTOMIZATION"
                                         Foreground="#333"
                                         FontSize="10"
                                         FontWeight="Bold"
                                         Margin="0,0,0,15"/>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                <Button Width="35" Height="35" Background="White" Click="whiteplz" Margin="0,0,10,0"/>
                                <Button Width="35" Height="35" Background="Cyan" Click="cyanplz" Margin="0,0,10,0"/>
                                <Button Width="35" Height="35" Background="Red" Click="redplz" Margin="0,0,10,0"/>
                                <Button Width="35" Height="35" Background="MediumPurple" Click="purpleplz" Margin="0,0,10,0"/>
                                <Button Width="35" Height="35" Background="LimeGreen" Click="greenplz" Margin="0,0,10,0"/>
                            </StackPanel>

                            <Button Content="OPEN DEBUG CONSOLE"
                                Click="showlogs"
                                Style="{StaticResource axiom_btn_bruh}"
                                Height="35"
                                Width="180"
                                HorizontalAlignment="Left"/>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Border>
</Window>
